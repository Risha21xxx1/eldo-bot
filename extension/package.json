{
  "name": "eldorado-extension",
  "version": "0.1.0",
  "description": "Chrome Manifest V3 extension for Eldorado.gg order monitoring",
  "private": true,
  "scripts": {
    "build": "tsc && npm run copy-assets",
    "copy-assets": "cp src/popup/index.html dist/popup/index.html && cp src/options/index.html dist/options/index.html && cp manifest.json dist/manifest.json && mkdir -p dist/icons && node scripts/create-icons.js",
    "watch": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/**/*.ts"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/chrome": "^0.0.260",
    "@types/node": "^20.10.0",
    "eslint": "^8.56.0",
    "@typescript-eslint/eslint-plugin": "^6.18.0",
    "@typescript-eslint/parser": "^6.18.0"
  }
}
